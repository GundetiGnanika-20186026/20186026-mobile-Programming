package com.example.android.sunshine;

import android.view.View;

interface ForecastAdapterViewHolder {
    // TODO (6) Override onClick, passing the clicked day's data to mClickHandler via its onClick method
    void onClick(View v);
}
